---
-    name: Enable ufw
     ufw:
         state: enabled
         policy: allow

-    name: Allow OpenSSH 22port
     ufw:
         rule: allow
         name: OpenSSH

-    name: Allow 80 port tcp
     ufw:
         rule: allow
         port: '80'
         proto: tcp

